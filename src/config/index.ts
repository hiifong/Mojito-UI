import { setupStore } from '@/config/modules/store'
import { setupI18n } from '@/config/modules/i18n'
import { setupRouter } from '@/config/modules/router'
import { setupElementPlus } from '@/config/modules/ElementPlus'
import { setupPrimeVue } from '@/config/modules/PrimeVue'
import { setupPowerBy } from '@/config/modules/PowerBy'

export { setupStore, setupI18n, setupElementPlus, setupPrimeVue, setupPowerBy, setupRouter }
